Review the AMD BC-250 Windows GPU driver at C:\AMD-BC-250\AMD-BC-250-Windows-Driver-main for correctness, using freshly-gathered Linux kernel ground truth. Do NOT modify any files — research and report only.

## New ground-truth facts to check against (these override old comments in the driver)

### 1. Linux cyan_skillfish golden register table (from gfx_v10_0.c, golden_settings_gc_10_0_cyan_skillfish[])
Linux programs ALL of these via plain WREG32 read-modify-write (soc15_program_register_sequence: if and_mask==0xffffffff write or_mask directly; else tmp=read; tmp&=~mask; tmp|=or_mask&mask; write). The FIRST entry writes GRBM_GFX_INDEX=0xE0000000 (broadcast select for all SE/SH), which is critical.

The full 34-entry table (name, mm, BASE_IDX, and_mask, or_val):
- GRBM_GFX_INDEX, mm=0x2200, BASE_IDX=1, 0xffffffff, 0xe0000000 (special: driver must use empirically-live address 0x34D0, NOT 0xA000+0x2200*4)
- GE_FAST_CLKS, 0x0fe8, 0, 0x3fffffff, 0x0000493e
- CGTT_CPF_CLK_CTRL, 0x50b1, 1, 0xfcff8fff, 0xf8000100
- CGTT_SPI_CLK_CTRL, 0x5080, 1, 0xff7f0fff, 0x3c000100
- CB_HW_CONTROL_3, 0x1423, 0, 0xa0000000, 0xa0000000
- CB_HW_CONTROL_4, 0x1422, 0, 0x00008000, 0x003c8014
- CH_DRAM_BURST_CTRL, 0x2d84, 1, 0x00000010, 0x00000017
- CH_PIPE_STEER, 0x2d90, 1, 0xffffffff, 0xd8d8d8d8
- CH_VC5_ENABLE, 0x2d94, 1, 0x00000003, 0x00000003
- CP_SD_CNTL, 0x1f57, 0, 0x800007ff, 0x000005ff
- DB_DEBUG, 0x13ac, 0, 0xffffffff, 0x20000000
- DB_DEBUG3, 0x13ae, 0, 0xffffffff, 0x00000200
- DB_DEBUG4, 0x13af, 0, 0xffffffff, 0x04800000
- DB_LAST_OF_BURST_CONFIG, 0x13ba, 0, 0xffffffff, 0x03860210
- GB_ADDR_CONFIG, 0x13de, 0, 0x0c1800ff, 0x00000044
- GCR_GENERAL_CNTL, 0x1583, 0, 0x00009d00, 0x00008500
- GCMC_VM_CACHEABLE_DRAM_ADDRESS_END, 0x1712, 0, 0xffffffff, 0x000fffff
- GL1_DRAM_BURST_CTRL, 0x2d04, 1, 0x00000010, 0x00000017
- GL1_PIPE_STEER, 0x2d10, 1, 0xfcfcfcfc, 0xd8d8d8d8
- GL2_PIPE_STEER_0, 0x2e25, 1, 0x77707770, 0x21302130
- GL2_PIPE_STEER_1, 0x2e26, 1, 0x77707770, 0x21302130
- GL2A_ADDR_MATCH_MASK, 0x2e21, 1, 0xffffffff, 0xffffffcf
- GL2C_ADDR_MATCH_MASK, 0x2e03, 1, 0xffffffff, 0xffffffcf
- GL2C_CGTT_SCLK_CTRL, 0x50ac, 1, 0x10000000, 0x10000100
- GL2C_CTRL2, 0x2e01, 1, 0xfc02002f, 0x9402002f
- GL2C_CTRL3, 0x2e0c, 1, 0x00002188, 0x00000188
- PA_SC_ENHANCE, 0x109c, 0, 0x08000009, 0x08000009
- PA_SC_BINNER_EVENT_CNTL_0, 0x106c, 0, 0xcc3fcc03, 0x842a4c02
- PA_SC_LINE_STIPPLE_STATE, 0x2281, 1, 0x0000000f, 0x00000000
- RMI_SPARE, 0x153f, 0, 0xffff3109, 0xffff3101
- SQ_ARB_CONFIG, 0x10ac, 0, 0x00000100, 0x00000130
- SQ_LDS_CLK_CTRL, 0x5090, 1, 0xffffffff, 0xffffffff
- TA_CNTL_AUX, 0x12e2, 0, 0x00030008, 0x01030000
- UTCL1_CTRL, 0x1588, 0, 0x00800000, 0x00800000

Address formula: BAR5 = (BASE_IDX ? AMDBC250_GC_BASE_SEG1(0xA000) : AMDBC250_GC_BASE(0x1260)) + mm*4. These are byte offsets into GPU BAR5 (0xFE800000).

### 2. Linux gfx_v10_0_hw_init order (DIRECT/SRIOV load):
1. golden registers (init_golden_registers)
2. SMU firmware load (amdgpu_pm_load_smu_firmware) + disable_gpa_mode (DIRECT only)
3. GRBM CAM remapping check/setup (if enable_grbm_cam_remapping)
4. constants_init: GRBM_CNTL READ_TIMEOUT=0xff; setup_rb; CU info; SH_MEM_CONFIG per VMID (nv_grbm_select me=0 pipe=0 queue=0 vmid=i); init_compute_vmid; init_gds_vmid
5. rlc_resume
6. cp_resume: cp_gfx_enable(false)/cp_compute_enable(false) halt first, load ME/PFP/CE microcode, MEC microcode, kiq_resume, kcq_resume, cp_gfx_resume, ring tests.

### 3. Empirically-confirmed register facts (do not challenge these)
- GC_BASE=0x1260, GC_BASE_SEG1=0xA000 (inc/amdbc250_dream_kmd.h:51-52)
- GRBM_GFX_INDEX live register is 0x34D0 on BC-250 (write 0xE0000000 broadcast, readback matches)
- CP_ME_CNTL = 0x1260 + 0x0E05*4 = 0x4A74
- COMPUTE DISPATCH_INITIATOR = 0x80E0 (=0x1260+0x1ba0*4), DIM_X=0x80E4, PGM_LO=0x8110, USER_DATA=0x81E0
- CP_HQD: MQD_BASE_ADDR=0x9104, HQD_ACTIVE=0x910C, PQ_BASE=0x9124, PQ_CONTROL=0x9148, PQ_WPTR_LO=0x91DC
- GB_ADDR_CONFIG: hw.h says 0x9800 but Linux formula gives 0x1260+0x13de*4=0x61D8. Both unverified at runtime. Flag this discrepancy.
- The IP discovery table (confirmed 2026-07-31): all our register bases (GC 0x1260, HDP 0xF20, MMHUB 0x1A000, DF 0x7000, OSSSYS 0x10A0, MP0 0x16000, THM 0x16600, SMUIO 0x16800/0x16A00, UMC0 0x14000, FUSE 0x17400) are CORRECT.
- SDMA registers live at GC offsets (SDMA ring init at 0xE018 worked).

## Files to review (read all of them)
- C:\AMD-BC-250\AMD-BC-250-Windows-Driver-main\src\kmd\amdbc250_dream_golden.c (currently only programs 1 register: CC_GC_SHADER_ARRAY_CONFIG at 0x9C1C, mask 0xFFFF0000 value 0xFFE00000, with a comment claiming registers 0x3400-0x8100 cause hardware freeze — this claim is now SUSPECT since Linux programs them fine via plain WREG32)
- C:\AMD-BC-250\AMD-BC-250-Windows-Driver-main\src\kmd\amdbc250_dream_hw_init.c (13-step init; steps: 1 mem controller, 2 golden, 3 HDP, 4 IH ring, 5 halt CP, 6 load fw, 7 GFX ring, 8 SDMA ring, 9 GART, 10 GPUVM, 11 display, 12 PSP, 13 RLC, 13b VRAM)
- C:\AMD-BC-250\AMD-BC-250-Windows-Driver-main\src\kmd\amdbc250_dream_kmd.c (335KB main driver file — read the register offset definitions, READ_REG/WRITE_REG, INIT_HARDWARE IOCTL handler, and any GRBM/COMPUTE/CP_HQD register usage)
- C:\AMD-BC-250\AMD-BC-250-Windows-Driver-main\src\kmd\amdbc250_dream_fw_load.c
- C:\AMD-BC-250\AMD-BC-250-Windows-Driver-main\inc\amdbc250_dream_hw.h (register offset definitions)
- C:\AMD-BC-250\AMD-BC-250-Windows-Driver-main\inc\amdbc250_dream_kmd.h

## What to check and report
1. Golden register programming: Does the driver program the full golden table? Is the GRBM_GFX_INDEX broadcast select (0xE0000000) written FIRST before other golden writes? Are golden register offsets computed correctly per the formula above? Is the freeze claim in golden.c justified or contradicted by Linux evidence?
2. Init ORDER: Compare our 13-step DreamV3HwInitialize sequence against Linux gfx_v10_0_hw_init order. Is anything missing (constants_init/SH_MEM_CONFIG/GRBM_CNTL READ_TIMEOUT, SMU first, disable_gpa_mode)? Is anything in the wrong order?
3. Register offsets: Verify all register offset macros in hw.h against the formula BAR5=(BASE_IDX?0xA000:0x1260)+mm*4 using the mm values in the golden table above. Flag any register that does not match (especially GB_ADDR_CONFIG 0x9800 vs 0x61D8).
4. IOCTL handlers in kmd.c: parameter validation, METHOD_BUFFERED buffer sharing bugs, pointer safety, integer overflow in offset+4 bounds checks, register offset range checks (0..0x80000).
5. Memory leaks, race conditions, error paths, uninitialized variables.
6. Whether the "HwUnhaltCp=0 default", "HwInitGart=0", "HwInitVm=0", "HwInitSdmaRing=0" kill-switch defaults are consistent with the Linux sequence (Linux DOES init GART/VM/SDMA as part of normal amdgpu init — but on BC-250 those steps caused 0x1A BSOD, so skipping them may be a Windows-specific necessity, not a bug).

Report findings as a numbered list, each with: severity (CRITICAL/HIGH/MEDIUM/LOW), file:line, description, and concrete fix suggestion. Prioritize correctness issues (wrong offsets, wrong order, missing critical init) over style. Be precise about line numbers.
Let me start by reading the files I need to review. This is a code review task on the AMD BC-250 Windows driver. I need to read the specified files and check against the Linux ground truth provided.

Let me read all the files in parallel.

I'll review the AMD BC-250 Windows driver against the freshly-gathered Linux ground truth. Let me start by reading all the specified files.

Let me continue reading the hw.h file and then the main driver files.

Let me continue reading the header and the main driver files.

Now let me read the main driver file, kmd.c. It's 335KB so I'll need to read it in chunks. Let me first check the file size and find the relevant sections.

Now let me examine the main driver file. It's large (335KB), so let me first find the key sections.

$ (Get-Item "src\kmd\amdbc250_dream_kmd.c").Length; (Get-Content "src\kmd\amdbc250_dream_kmd.c" | Measure-Object -Line).Lines

335695
6703
Let me continue reading the kmd.h file and then the main kmd.c file. I need to find the READ_REG/WRITE_REG functions, the INIT_HARDWARE IOCTL handler, and GRBM/COMPUTE/CP_HQD register usage in kmd.c.

Let me search for key sections in kmd.c.

The kmd.c is 6703 lines. Let me read the key sections: register access functions, INIT_HARDWARE IOCTL handler, and IOCTL handlers. Let me first find the DreamV3ReadRegister/DreamV3WriteRegister definitions.

Let me find the key function definitions in kmd.c.

Let me find the actual definition of DreamV3ReadRegister/DreamV3WriteRegister in kmd.c, and the READ_REG/WRITE_REG IOCTL handlers, and the INIT_HARDWARE IOCTL handler.

Let me find the DreamV3ReadRegister/DreamV3WriteRegister definitions and the main IOCTL dispatch table.

The DreamV3ReadRegister/DreamV3WriteRegister definitions must be somewhere. Let me search with a different pattern - maybe they're defined in a header. Let me search all files for the definition (not usage).

Let me find where DreamV3ReadRegister/DreamV3WriteRegister are actually defined.

I've confirmed that the golden registers like GE_FAST_CLKS, CGTT_CPF, CGTT_SPI, CB_HW_CONTROL, PA_SC_ENHANCE, UTCL1_CTRL, DB_DEBUG, GL2C_CTRL, GCR_GENERAL are NOT defined in the driver's headers at all. The golden.c only programs ONE register (CC_GC_SHADER_ARRAY_CONFIG), whereas Linux programs 34 golden registers.

Now let me compile the full review. Let me structure my findings.

Key findings summary:

CRITICAL/High:

Golden table completely missing — only 1 of 34+ registers programmed. The header comment in golden.c claims "Linux programs these via GRBM_CAM_INDEX/DATA mechanism" which is FALSE per ground truth (Linux uses plain WREG32 RMW). The entire "freeze zone" theory in golden.c (0x3400-0x8100 causes hardware freeze) is unverified and contradicts Linux ground truth — Linux programs GE_FAST_CLKS (0x4C00), CGTT_CPF_CLK_CTRL (0x1C2C4), CGTT_SPI_CLK_CTRL (0x1C200), CB_HW_CONTROL_3/4 (0x62EC/0x62E8), DB_DEBUG (0x6110), DB_DEBUG3 (0x6118), DB_DEBUG4 (0x611C), PA_SC_ENHANCE (0x54D0), etc. all via direct WREG32.
GB_ADDR_CONFIG offset wrong: hw.h:657 says 0x9800, Linux ground truth says mm=0x13DE → BAR5 = 0x1260 + 0x4F78 = 0x61D8. This is a confirmed mismatch. Also the value written 0x00100044 vs Linux golden 0x00000044.
UNLOCK_40CU handler has contradictory comments (says 0x3264 in one comment, but uses AMDBC250_REG_CC_GC_SHADER_ARRAY_CONFIG which is 0x9C1C correct; SPI comment says 0x34FC but constant is 0x5C3C). The comments are stale/wrong but the code writes correct addresses. However SPI write 0x3F00 vs Linux 0x1F... AGENTS.md says BC-250 WGP bits are 8-13 (0x3F00), that's consistent with their testing. But per new ground truth, Linux duggasco writes 0x1F for SPI_PG. Hmm, the header comment in UNLOCK_40CU says "BC-250: 0x34FC" and "bits 8-13 (0x3F00)". This is a discrepancy: prior session notes say 0x34FC was "partially writable" SPI_PG_MASK, while 0x5C3C is SPI_PG_ENABLE_STATIC_WGP_MASK (read-only 0). So the comments are mixed up but the code uses the constants from hw.h. The important note: per the newest 2026-07-31 findings, the SPI_PG register is per-bank GRBM-indexed and Linux stock reads 0x07, but driver read 0x00000000. This means the UNLOCK_40CU IOCTL writes 0x3F00 to 0x5C3C but may need per-bank GRBM select first — the handler doesn't set GRBM_GFX_INDEX at all before writing. This is a real gap for the 40CU unlock path.
KIQ_BIOS_RING_SUBMIT uses GCVM_CONTEXT0_CNTL at 0xB460 and PT_BASE_LO at 0x6C8C. These offsets: Linux mmGCVM_CONTEXT0_CNTL = 0x2D18? Wait let me check. hw.h:556 AMDBC250_REG_GCVM_CONTEXT0_CNTL = 0x00000B460. The AGENTS.md note says "GCVM_CONTEXT0_CNTL 0x0B460 = mm4 WITHOUT GC base -- potential missing 0x1260". Actually 0xB460... if mm=0x2D18, then 0x2D184 = 0xB460. And GC_BASE + 0x2D184 = 0x1260+0xB460 = 0xC6C0. So the define at 0xB460 is mm4 without GC base (or is it with?). Let me compute: GC_BASE=0x1260. 0x1260 + 0xB460 = 0xC6C0. So 0xB460 itself = 0x2D184 = 0xB460, which does NOT include the GC base. But the comment in hw.h says "GC_BASE-shifted". Wait, but other GCVM_CONTEXT0_PT_BASE_LO = 0x6C8C. Linux mmGCVM_CONTEXT0_PT_BASE_LO = 0x1A0B? 0x1260 + 0x1A0B4 = 0x1260+0x682C = 0x7A8C. Hmm, that doesn't match 0x6C8C. Let me try: 0x6C8C - 0x1260 = 0x5A2C; 0x5A2C/4 = 0x168B. So mmGCVM_CONTEXT0_PT_BASE_LO = 0x168B? Actually for gc_10_1_0_offset.h, mmGCVM_CONTEXT0_PT_BASE_LO is 0x1A0B for Navi10? Not sure. The AGENTS.md bug #6 says "GCVM invalidate regs mismatch (hw.h 0x0B51C vs working 0x6C0C)". 0x6C0C-0x1260 = 0x59AC, /4 = 0x166B. So mmGCVM_CONTEXT0_PT_INVALIDATE_LO = 0x166B. And CONTEXT0_CNTL at 0xB460: 0xB460-0x1260 = 0xA200, /4 = 0x2880. So mmGCVM_CONTEXT0_CNTL = 0x2880? Hmm, that doesn't match the Linux mm value. Actually Linux gc_10_1_0_offset.h: mmGCVM_CONTEXT0_CNTL = 0x2D18? 0x2D184 = 0xB460. So if Linux mm is 0x2D18, then raw = 0xB460 WITHOUT GC base. This is exactly the "potential missing 0x1260" flag in my summary. But wait, the hw.h comment says "GC_BASE-shifted" — the define value is 0xB460 and it does NOT include the 0x1260. So this appears to be a bug: GCVM_CONTEXT0_CNTL should be at 0xC6C0 (0x1260 + 0xB460) if mm is 0x2D18. However, I'm not 100% sure of the Linux mm value. Given the AGENTS.md-verified GCVM_CONTEXT0_PT_BASE_LO = 0x6C8C and INVALIDATE = 0x6C0C both in the 0x6Cxx range (GC_BASE shifted: 0x1260 + 0x59xx = 0x6Cxx), while CONTEXT0_CNTL at 0xB460 is in a completely different range — that's suspicious. The task summary said "BIOS_GCVM_CONTEXT0_CNTL 0x0B460 = mm4 WITHOUT GC base -- potential missing 0x1260". I'll report this as a HIGH/CRITICAL offset concern to verify.
Actually, wait. Let me reconsider. In Navi10/gfx10, mmGCVM_CONTEXT0_CNTL in gc_10_1_0_offset.h. I recall for gfx10.1 it might be 0x2D18. Hmm. Actually the raw navi10 offset table: GCVM_CONTEXT0_CNTL = 0x2D18? I don't have the exact table. But the driver's own hw.h:556 comment claims these are GC_BASE-shifted, and 0xB460 ≠ 0x1260 + something * 4 in the context regs area (0x6Cxx). There's a real inconsistency: the context registers (0x6C0C, 0x6C10, 0x6C8C) are all in 0x6Cxx range, but CONTEXT0_CNTL at 0xB460 is far away. That's a strong smell. Linux gfx_v10_0.c writes GCVM_CONTEXT0_CNTL via WREG32_SOC15(GC, 0, mmGCVM_CONTEXT0_CNTL, 1). In gc_10_1_0_offset.h, mmGCVM_CONTEXT0_CNTL = 0x2D18 (0x2D184 = 0xB460). So raw mm4 = 0xB460, and the register offset used by WREG32_SOC15 is adev->reg_offset[GC_HWIP][0] + 0xB460 = 0x1260 + 0xB460 = 0xC6C0. So the driver's 0xB460 is WRONG — it's missing the 0x1260 GC base. Same for the KIQ_BIOS_RING_SUBMIT code which writes 0x0B460 directly.

Similarly GCVM_CONTEXT0_PT_BASE_LO: Linux mm = 0x1A0B? No. For gfx10.1, mmGCVM_CONTEXT0_PT_BASE_LO might be 0x168B? 0x168B4 = 0x5A2C, +0x1260 = 0x6C8C. YES! That matches 0x6C8C. So PT_BASE_LO = 0x1260 + 0x168B4 = 0x6C8C is correct (GC_BASE shifted). And CONTEXT0_CNTL should be 0x1260 + mm4. If mmGCVM_CONTEXT0_CNTL = 0x2880 then 0x1260+0xA200 = 0xB460 which would make 0xB460 correct. Hmm, conflicting. Let me reconsider: 0x28804 = 0xA200, +0x1260 = 0xB460. So if mmGCVM_CONTEXT0_CNTL = 0x2880, then 0xB460 IS correct (GC-shifted).

Which is right? I don't have the exact gc_10_1_0_offset.h at hand. But the pattern of the driver's own data: 0x6C0C (invalidate LO), 0x6C10 (invalidate HI), 0x6C8C (PT_BASE_LO), 0x6C90 (PT_BASE_HI). These are close together (0x6C0C to 0x6C90). If mmGCVM_CONTEXT0_CNTL were 0x2880 it'd be far from PT_BASE_LO 0x168B, which is unusual (registers in same block are usually close). Actually in the gc_10_1_0_offset.h, GCVM_CONTEXT0_CNTL comes before GCVM_CONTEXT0_PT_BASE_LO in the enum, and they're usually a few entries apart (CNTL, PT_BASE_LO, PT_BASE_HI, PT_INVALIDATE_LO, PT_INVALIDATE_HI, ...). So they should be close in mm space. 0x2880 is much higher than 0x168B. That supports the "0xB460 is missing GC base" theory: mmGCVM_CONTEXT0_CNTL is likely 0x2D18 or similar, raw = 0xB460, and correct BAR5 = 0xC6C0.

Given the ambiguity, I'll flag it as HIGH priority "verify" with the strong note that it's likely missing +0x1260.

METHOD_BUFFERED buffer sharing in the KIQ_BIOS_RING_SUBMIT handler (kmd.c:6225): The handler reads input BEFORE zeroing output — this was fixed (line 6258-6267 reads input, then zeroes). Good — the fix is present. But note in the actual code at 6229, it checks outputLen but reads resp as outputBuffer, and it checks inputLen separately. This looks fixed. However — wait, the code writes resp->ScratchBefore etc. after RtlZeroMemory, and it reads ringPa from input before zeroing. That matches the documented fix. OK.
But there's a bug: at line 6229 it only checks outputLen >= sizeof(...) but reads inputBuffer at line 6261 guarded by inputLen check. Good.

The DreamV3DeviceControl dispatch: does it validate that inputBuffer/outputBuffer are non-NULL when METHOD_BUFFERED? SystemBuffer is always non-NULL for METHOD_BUFFERED with adequate size. But if inputLen == 0 the driver may still read. Most handlers check inputLen. Some don't — e.g., 0x900 BAR5_PROXY_READ checks inputLen >= sizeof(ULONG). Good.
INIT_HARDWARE (0x80000B80): accepts inputLen 16..sizeof. When MmioPhysicalBase==0, auto-detect. If HardwareInitialized already, it re-inits (unmaps and remaps). Race: two threads could call INIT_HARDWARE concurrently → double mapping/leak. There's a DeviceMutex (ExInitializeFastMutex) but does INIT_HARDWARE use it? Need to check. Actually I saw earlier the code around 3708. Let me check whether it acquires DeviceMutex. I didn't see a KeAcquireFastMutex in the INIT handler. Potential race on re-init. I'll mention as MEDIUM.
READ_REG/WRITE_REG bounds checks: DevExt->MmioSize < 4 || RegAcc->RegisterOffset > DevExt->MmioSize - 4 — this is safe (checks MmioSize < 4 first, avoiding underflow). Good. BAR5 proxy uses offset <= 0x80000 - sizeof(ULONG) — safe. Good — these were the fixes from prior review, and they're correct.
SW PM4 executor: IT_WRITE_DATA register path: if (isReg || (addrLo < 0x200000)) then writes to addrLo + d*4 via DreamV3WriteRegister WITHOUT bounds check against 0x80000. addrLo could be up to 0x1FFFFC, but MmioSize is 0x80000. So a WRITE_DATA to addrLo in range 0x80000..0x200000 would write beyond the mapped BAR5! DreamV3WriteRegister does WRITE_REGISTER_ULONG at MmioVirtualBase + offset with NO bounds check. This is a potential OOB MMIO write → system crash/BSOD or arbitrary access. However, does the MMIO mapping extend beyond 0x80000? MmMapIoSpace maps 512KB (0x80000) from 0xFE800000. So writing at offset 0x100000 would go beyond the mapping → page fault at elevated IRQL → bugcheck. This is a real safety issue in the SW PM4 executor. The register-range checks for SET_CONFIG_REG / SET_CONTEXT_REG / SET_SH_REG compute baseReg = GC_BASE + (hwOff << 2) without bounds check either. A malformed PM4 could write anywhere in (roughly) the mapped 512KB, but more importantly hwOff up to 0xFFFF would give baseReg up to 0x1260+0x3FFFC = 0x4125C < 0x80000 — actually within bounds for 16-bit offsets. But hwOff is ULONG (32-bit) from the command buffer! ULONG hwOff = Commands[i]; ULONG baseReg = AMDBC250_GC_BASE + (hwOff << 2); — hwOff is 32-bit, so hwOff<<2 could overflow and baseReg could be huge → OOB write. Definitely a bug: no bounds check on hwOff in SET_CONFIG/SET_CONTEXT/SET_SH handlers. For DISPATCH_DIRECT it writes fixed known offsets — OK. For PM4_TYPE_0, baseReg = (